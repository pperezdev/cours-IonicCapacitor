import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'supinfo.ionic.github-stats',
  appName: 'GitHub Stats',
  webDir: 'dist'
};

export default config;
